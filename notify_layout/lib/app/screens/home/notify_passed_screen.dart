import 'package:flutter/material.dart';

class NotifyPassedScreen extends StatelessWidget {
  const NotifyPassedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
