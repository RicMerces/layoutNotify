import 'package:flutter/material.dart';

class NotifyRecurrentScreen extends StatelessWidget {
  const NotifyRecurrentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
